import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary palette from design/colors.txt
  static const Color lightLavender = Color(0xFFF0EEFF);
  static const Color deepNavy = Color(0xFF1E3A5F);
  static const Color warmOrange = Color(0xFFFFB347);
  static const Color softBlue = Color(0xFF4A8FE7);

  // Auth screen gradient
  static const Color gradientTop = Color(0xFF708898);
  static const Color gradientBottom = Color(0xFFAABDCE);

  // Common
  static const Color white = Color(0xFFFFFFFF);

  // Input fields
  static const Color inputBackground = Color(0x33FFFFFF);
  static const Color inputBorder = Color(0x55FFFFFF);
  static const Color hintColor = Color(0x99FFFFFF);

  // Overlay
  static const Color darkOverlay = Color(0x88000000);
}
